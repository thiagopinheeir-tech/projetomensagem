[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = [
  'apt-get update',
  'apt-get install -y chromium chromium-sandbox',
  'npm ci'
]

[phases.build]
cmds = [
  'echo "Build complete"'
]

[start]
cmd = 'node server.js'
