# ============ SERVIDOR ============
PORT=5000
NODE_ENV=development

# ============ BANCO DE DADOS (PostgreSQL Local) ============
DATABASE_URL=postgresql://postgres:postgres123@localhost:5432/top_active_whatsapp
DB_HOST=localhost
DB_PORT=5432
DB_NAME=top_active_whatsapp
DB_USER=postgres
DB_PASSWORD=postgres123

# ============ SUPABASE (Nuvem - Opcional) ============
# Obtenha em: https://supabase.com
SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_ANON_KEY=sua-chave-anonima-aqui
SUPABASE_SERVICE_KEY=sua-chave-servico-aqui

# ============ JWT (SEGURANÇA) ============
# Gerar uma chave segura com:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=uma_chave_bem_grande_e_aleatoria_mude_isto_em_producao_min_32_caracteres
JWT_EXPIRATION=7d

# ============ CRIPTOGRAFIA (chaves/tokens) ============
# IMPORTANTE: Defina uma chave fixa em produção, senão chaves/tokens salvos não poderão ser descriptografados após restart.
# Gerar com:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# ============ CORS ============
CORS_ORIGIN=http://localhost:3000,http://localhost:5173

# ============ IA DO CHATBOT ============
# OPÇÃO 1: OpenAI (Recomendado)
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_TEMPERATURE=0.7
OPENAI_MAX_TOKENS=300


# ============ CONFIGURAÇÃO DO CHATBOT - EMPRÉSTIMO PESSOAL ============
BUSINESS_NAME=JP Financeira
BUSINESS_DESCRIPTION=Empresa especializada em empréstimo pessoal rápido e seguro. Aprovamos seu crédito em até 24 horas com as melhores taxas do mercado. Atendimento de segunda a sábado, das 8h às 18h.
BUSINESS_SERVICES=Empréstimo Pessoal,Crédito Rápido,Antecipação de Recebíveis,Refinanciamento
BUSINESS_TONE=amigavel
BUSINESS_INSTRUCTIONS=Você é um atendente especializado em empréstimo pessoal. SEMPRE colete: nome completo, CPF, valor desejado e prazo antes de prosseguir. Informe valores e taxas quando solicitado. Seja claro sobre documentação necessária. NUNCA prometa aprovação garantida. Atendimento apenas Segunda a Sábado, 8h-18h.

# ============ RESPOSTAS PADRÃO - EMPRÉSTIMO PESSOAL ============
RESPONSE_PRECO=Oferecemos empréstimo pessoal de R$ 500 a R$ 50.000 com taxa a partir de 2,5% ao mês. O valor final depende da análise de crédito. Quer que eu te ajude a simular um valor?
RESPONSE_SITE=Visite nosso site: www.jpfinanceira.com.br ou acesse pelo WhatsApp mesmo! Posso te ajudar agora 😊
RESPONSE_TESTE=Para solicitar seu empréstimo, preciso de algumas informações: nome completo, CPF, valor desejado e prazo de pagamento. Posso te ajudar agora?

# ============ AGENDAMENTO (Google Agenda) ============
# Ative para permitir que o bot consulte horários disponíveis e crie eventos automaticamente.
GOOGLE_CALENDAR_ENABLED=false

# ID do calendário (ex: "seuemail@gmail.com" ou o ID do calendário compartilhado)
GOOGLE_CALENDAR_ID=

# Credencial de Service Account (ESCOLHA 1):
# Coloque o JSON completo em UMA linha (sem quebras). Ideal para produção/CI.
GOOGLE_SERVICE_ACCOUNT_JSON=

# Credencial de Service Account (ESCOLHA 2):
# Caminho para o arquivo JSON (ex: "secrets/google-service-account.json")
GOOGLE_SERVICE_ACCOUNT_FILE=

# Config do atendimento
BOOKING_ENABLED=false
BOOKING_ALWAYS_ON=false
BOOKING_TIMEZONE=America/Sao_Paulo
BOOKING_DURATION_MINUTES=30
BOOKING_OPEN_HOUR=9
BOOKING_CLOSE_HOUR=20

# ============ GOOGLE OAUTH (por usuário) ============
# Você cria no Google Cloud Console (OAuth Client ID).
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
# Em dev/electron local, pode manter assim:
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:5000/api/google/oauth/callback
# Opcional (se quiser customizar):
GOOGLE_OAUTH_SCOPES=https://www.googleapis.com/auth/calendar
